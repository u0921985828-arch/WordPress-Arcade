# Motores y runtime: la URL siempre lleva ?v=<hash>, así que se pueden guardar para siempre.
<IfModule mod_headers.c>
  <FilesMatch "\.js$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>
